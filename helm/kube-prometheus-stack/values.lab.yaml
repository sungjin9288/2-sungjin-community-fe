grafana:
  adminPassword: "ChangeMeBeforeRealUse!"
  service:
    type: ClusterIP
  additionalDataSources:
    - name: Loki
      type: loki
      access: proxy
      url: http://loki-stack.loki.svc.cluster.local:3100
      isDefault: false
      editable: true

prometheus:
  prometheusSpec:
    retention: 7d

alertmanager:
  enabled: false
